/* THIS FILE WAS GENERATED AUTOMATICALLY BY PAYLOAD. */
/* DO NOT MODIFY IT BECAUSE IT COULD BE REWRITTEN AT ANY TIME. */
import React from "react";

import "./custom.scss";

type Args = {
  children: React.ReactNode;
};

// Important:
// Your app's root `src/app/layout.tsx` now uses Payload's document-level `RootLayout`,
// which renders the only valid <html>/<body>. This nested route-group layout must
// only provide page-level styling and context.
export default function Layout({ children }: Args) {
  return children;
}

